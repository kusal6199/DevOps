spring:
  application:
    name: CrudStudentApi

  main:
    web-application-type: servlet

  datasource:
    url: jdbc:sqlserver://host.docker.internal:1433;databaseName=springApp;encrypt=false
    username: sa
    password: "@Kushal6!"
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081